<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Estate Property Template -->
    <template id="report_property_offers_template">
        <t t-foreach="docs" t-as="property">
            <t t-call="web.html_container">
                <t t-call="web.external_layout">
                    <div class="page">
                        <h2>
                            <span t-field="property.name" />
                        </h2>
                        <div>
                            <strong>Expected Price: </strong>
                            <span t-field="property.expected_price" />
                        </div>
                        <div>
                            <strong>Status: </strong>
                            <span t-field="property.state" />
                        </div>
                        <t t-call="estate.report_property_offers_table">
                            <t t-set="offers" t-value="property.mapped('offer_ids')" />
                        </t>
                    </div>
                </t>
            </t>
        </t>
    </template>


    <!-- Seller Offer Template -->
    <template id="report_seller_offers_template">
        <t t-foreach="docs" t-as="seller">
            <t t-call="web.html_container">
                <t t-call="web.external_layout">
                    <div class="page">
                        <h1>
                            <strong>Salesman:</strong>
                            <span t-field="seller.name" />
                        </h1>
                        <t t-foreach="seller.property_ids" t-as="property">
                            <div class="mb-5">
                                <h2>
                                    <span t-field="property.name" />
                                </h2>
                                <div>
                                    <strong>Expected Price: </strong>
                                    <span t-field="property.expected_price" />
                                </div>
                                <div>
                                    <strong>Status: </strong>
                                    <span t-field="property.state" />
                                </div>
                                <t t-call="estate.report_property_offers_table">
                                    <t t-set="offers" t-value="property.mapped('offer_ids')" />
                                </t>
                            </div>

                        </t>
                    </div>
                </t>
            </t>
        </t>
    </template>

    <!-- Sub Template -->
    <template id="report_property_offers_table" name="report_property_offers_table">
        <t t-if="offers">
            <table class="table">
                <thead>
                    <tr class="fw-bold">
                        <th>Price</th>
                        <th>Partner</th>
                        <th>Validity (days)</th>
                        <th>Deadline</th>
                        <th>State</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="offers" t-as="offer">
                        <td>
                            <span t-field="offer.price" />
                        </td>
                        <td>
                            <span t-field="offer.partner_id.name" />
                        </td>
                        <td>
                            <span t-field="offer.validity" />
                        </td>
                        <td>
                            <span t-field="offer.date_deadline" />
                        </td>
                        <td>
                            <span t-field="offer.status" />
                        </td>
                    </tr>
                </tbody>
            </table>
        </t>
        <t t-else="">
            <p>No offer has been made yet</p>
        </t>
    </template>

</odoo>