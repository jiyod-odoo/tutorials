<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Input Form -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="My new house">
                <header>
                    <button name="handle_sold" type="object" string="Sold"
                        invisible="state == 'sold'" class="bg-primary text-white" />
                    <button name="handle_cancel" type="object" string="Cancel"
                        invisible="state == 'sold'" />
                    <field name="state" readonly="True" widget="statusbar"
                        statusbar_visible="new, offer received, offer accepted, sold" />
                </header>
                <sheet>
                    <h1>
                        <field name="name" />
                    </h1>
                    <h4>
                        <field name="tag_ids" widget="many2many_tags"
                            options="{'color_field': 'color'}" />
                    </h4>
                    <group>
                        <group>
                            <field name="property_type_id"
                                options="{'no_create': true, 'no_open': true}" />
                            <field name="postcode" />
                            <field name="available_from" />
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="best_offer" />
                            <field name="selling_price" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <group>
                                    <field name="description" />
                                    <field name="bedrooms" />
                                    <field name="living_area" />
                                    <field name="facades" />
                                    <field name="garage" />
                                    <field name="garden" />
                                    <field name="garden_area" invisible="not garden" />
                                    <field name="garden_orientation" invisible="not garden" />
                                    <field name="total_area" />
                                </group>
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids"
                                readonly="state not in ('new', 'offer received')">
                                <list />
                            </field>
                        </page>
                        <page string="Other Info">
                            <group>
                                <group>
                                    <field name="seller_id" />
                                    <field name="buyer_id" />
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>


    <!-- Estate Tree -->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="estate_property_list"
                decoration-success="state in ('offer received','offer accepted')"
                decoration-muted="state == 'sold'"
                decoration-bf="state == 'offer accepted'"
            >
                <field name="name" width="200px"/>
                <field name="property_type_id" />
                <field name="postcode" />
                <field name="tag_ids" widget="many2many_tags" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="available_from" optional="True" />
            </list>
        </field>
    </record>

    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" groups_draggable="False" records_draggable="False">
                <field name="state" />
                <templates>
                    <t t-name="card">
                        <div class="fw-bold">
                            <field name="name" />
                        </div>

                        <!-- Use utility classes to space elements within one line -->
                        <div class="d-flex flex-column justify-content-between align-items-start">
                            <span class="text-secondary-emphasis">Expected Price: <field name="expected_price" /></span>

                            <t t-if="record.state.raw_value == 'offer received'">
                                <span class="text-secondary-emphasis">Best Offer: <field
                                        name="best_offer" /></span>
                            </t>
                            <t t-if="record.state.raw_value == 'offer accepted'">
                                <span class="text-secondary-emphasis">Selling Price: <field
                                        name="selling_price" /></span>
                            </t>
                            <field class="mt-2" name="tag_ids" />
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Search Bar -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Estate Search">
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area"
                    filter_domain="['|', ('living_area', '>', self), ('living_area', '=', self)]"
                />
                <field name="facades" />
                <separator />
                <filter string="Available" name="filter_state"
                    domain="[('state', 'in', ('new', 'offer received'))]" />
                <filter string="Postcode" name='group_by_postcode'
                    context="{'group_by':'postcode'}" />
            </search>
        </field>
    </record>

    <!-- Configuration Estate Property -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Real Estate</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_filter_state': True}</field>
    </record>

    <!-- Configuration Estate Offer -->
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Offer</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>