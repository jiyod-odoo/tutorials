<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="group_sale_team_lead" model="res.groups">
        <field name="name">User: Team Leader</field>
        <field name="sequence">15</field>
        <field name="privilege_id" ref="sales_team.res_groups_privilege_sales" />
        <field name="implied_ids" eval="[(4, ref('sales_team.group_sale_salesman'))]" />
        <field name="comment">the user will have access to his own and all members data in the sales
            application.</field>
    </record>

    <data noupdate="1">
        <record id="crm_rule_team_leader" model="ir.rule">
            <field name="name">Team Leader</field>
            <field ref="sales_team.model_crm_team" name="model_id" />
            <field name="domain_force">['|',('user_id','=',user.id),('user_id','=',False)]</field>
            <field name="groups" eval="[(4, ref('group_sale_team_lead'))]" />
        </record>
        <record id="crm_rule_team_leader_sale_order" model="ir.rule">
            <field name="name">Team Leader Sale Orders</field>
            <field ref="sale.model_sale_order" name="model_id" />
            <field name="domain_force">[('team_id.user_id','=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_sale_team_lead'))]" />
        </record>

    </data>
</odoo>